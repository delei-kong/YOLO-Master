
# 1. 创建 tmux 会话 持久化运行
tmux new -s train_v01

1. 激活环境
conda activate yolo_master
cd /root/workspace/YOLO-Master

2. 训练前检查环境
# 确认模型配置能正常 build
python scripts/reproduce/reproduce_visdrone.py --check-build

# 预览参数，确认一切正确
python scripts/reproduce/reproduce_visdrone.py --dry-run --epochs 100 --batch 32

3. 训练
// 可以先跑一次冒烟测试，就是epoch为1
这个是训练指令，通过 mosel 参数控制 v0.1-N 和 EsMOE-N
python scripts/reproduce/reproduce_visdrone.py --model v0.1-N --epochs 100 --batch 32 --device 0 --no-wandb --cache ram

# 第一轮：v0.1-N（无 MoE 稀疏问题，不需要 --no-sparse-eval）
python scripts/reproduce/reproduce_visdrone.py \
--model v0.1-N \
--epochs 2 \          
--batch 32 \
--device 0 \
--no-wandb \
--cache ram

# 第二轮：EsMoE-N（必须加 --no-sparse-eval，否则 val mAP 崩塌）
python scripts/reproduce/reproduce_visdrone.py \
--model EsMoE-N \
--epochs 100 \
--batch 32 \
--device 0 \
--no-wandb \
--no-sparse-eval \
--cache ram

1.
ls /root/workspace/YOLO-Master/ultralytics/cfg/datasets/GlobalWheat2020.yaml

2. 
python scripts/reproduce/reproduce_wheat.py --check-build

3. 
python -c "from ultralytics.data.utils import check_det_dataset; check_det_dataset('GlobalWheat2020.yaml', autodownload=True)"

wget -P /root/gpufree-data/datasets/ "https://gh-proxy.org/https://github.com/ultralytics/assets/releases/download/v0.0.0/GlobalWheat2020_labels.zip"

wget -P /root/gpufree-data/datasets/ \
    "https://zenodo.org/record/4298502/files/global-wheat-codalab-official.zip"

grep -Eo 'https://[^"() ]+' /root/workspace/YOLO-Master/ultralytics/cfg/datasets/construction-ppe.yaml

wget -P /root/gpufree-data/datasets/ "https://gh-proxy.org/https://github.com/ultralytics/assets/releases/download/v0.0.0/construction-ppe.zip"

python -c "from ultralytics.data.utils import check_det_dataset; check_det_dataset('construction-ppe.yaml', autodownload=True)"

python scripts/reproduce/reproduce_ppe.py --model v0.1-N --epochs 50 --batch 32 --device 0


登录
wandb login


1. 停了当前的 50 epoch 测试（Ctrl+C），跑正式的 100 epoch 全量训练。同时创建 logs 目录：

mkdir -p /root/workspace/docs/issue6/logs/ppe

2. 清掉测试 run，跑全量训练：

rm -rf /root/workspace/YOLO-Master/runs/reproduce/ppe/construction-ppe_v0.1-N

python scripts/reproduce/reproduce_ppe.py \
--model v0.1-N --epochs 100 --batch 32 --device 0

3. 关于产物收集——_reproduce_common.py 自动产的东西已经很全了：

┌─────────────────┬───────────────────────────┬─────────────────────────────────┐
│ 自动生成的产物  │           位置            │              用途               │
├─────────────────┼───────────────────────────┼─────────────────────────────────┤
│ results.csv     │ runs/reproduce/ppe/<run>/ │ per-epoch 完整指标，PR 核心数据 │
├─────────────────┼───────────────────────────┼─────────────────────────────────┤
│ results.png     │ 同上                      │ 训练曲线图                      │
├─────────────────┼───────────────────────────┼─────────────────────────────────┤
│ args.yaml       │ 同上                      │ 完整训练参数，可复现证明        │
├─────────────────┼───────────────────────────┼─────────────────────────────────┤
│ weights/best.pt │ 同上                      │ 最优模型                        │
├─────────────────┼───────────────────────────┼─────────────────────────────────┤
│ W&B 日志        │ 云端                      │ 在线训练曲线，PR 中贴链接       │
└─────────────────┴───────────────────────────┴─────────────────────────────────┘

训练完成后跑 --summary-only 生成汇总表：

python scripts/reproduce/reproduce_ppe.py --summary-only

然后我把 runs/reproduce/ppe/ 下所有产物统一拷贝到 docs/issue6/logs/ppe/。

你先把 50 epoch 的停了，跑起来 100 epoch 的，告诉我。

python scripts/reproduce/reproduce_construction_ppe.py --model EsMoE-N --epochs 200 --batch 32 --device 0 --no-sparse-eval


